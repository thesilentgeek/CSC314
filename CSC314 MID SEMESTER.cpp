#include <string>
#include <algorithm>
#include <iostream>
int main()
{
    std::string enter a word;
    std::cin >> enter a word;
    if( equal(s.begin(), s.begin() + s.size()/2, s.rbegin()) )
        std::cout << "is a palindrome.\n";
    else
        std::cout << "is NOT a palindrome.\n";
}
